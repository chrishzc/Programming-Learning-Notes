深度殘差網路（Deep residual network，ResNet） • 前面介紹了AlexNet(7層)、VGG(19層)、GoogLeNet(22層)等網絡模 型，在這些模型的依序出現中我們發現，當網路越來越深時，網路 可以進行更加複雜的特徵的抽取，因此理論上當模型越深時應該會 有更好的辨識結果。 • 問題：梯度消失 • 解決辦法：改變激勵函數(使用relu)、權重正則化等等。
另外一個問題：隨著網路加深，出現訓練集準確率下降的現象(退化問題)

如何解決退化問題 • 想法：既然深層網路相比於淺層網路具有退化問題，那麼是否可以 保留深層網路的深度，又可以有淺層網路的優勢去避免退化問題呢？ • 作法：如果將深層網路的後面若干層學習成恆等映射 ( ) h x x= ，那麼模型就退化成淺層網路。 • 但是直接去學習這個恆等映射是很困難的
換一種方式，把網路設計成： = ( ) ( ) H x F x x Fx Hx x ( ) +  = − 只要 Fx = ( ) 0 就構成了一個恆等映射 ( ) ( ) H x x= Fx 這裡的 為殘差

而ResNet提出的初心也很簡單，層次變深，哪怕啥效果都沒有，起碼不會變差吧，於是何愷明博士提 出了恆等變換，就是後面的層次最差的狀況就是沒有用處。


殘差學習的結構如圖下所示，這有點類似與電路中的『短路』接法， 所以是一種短路連線（shortcut connection）。 x 殘差學習的block一共包含兩個分支或者兩種對映 （mapping）： 1. identity mapping：也就是 (彎曲的那條線)， 為 淺層(前一層)輸出，而identity mapping 的意思就是 指自己本身的對應。 2. residual mapping：也就是 F(x) x F(x) x ， 為中間兩層 卷積層對 計算後的輸出，這部分稱為殘差對映。

在實踐殘差學習的block時，這邊的Short Connection 分成兩種方式： F(x) x y F(x,W) x • (1)Short cut 如果同維度映射， 與 相加就是逐元素相加，這時結果應 為： i x • (2)如果兩者維度不同，需要給 執行一個線性映射來轉換成匹配的維度，這 時結果應為： y F(x,W) Wx i s W ，其中 是線性投影函數，主要讓x與F的 s 維度可以匹配。

兩種實作residual模塊 兩種不同的residual模塊 參數數目：3x3x256x256x2=1179648(如果輸入 與輸出與卷積核的數目都是 256的話) 1x1卷積核來增加非線性和減小輸出的參數量 參數數目：1X1x256x64+3x3x64x64+1x1x64x256 = 69632

Basic Block 實現
利用堆疊方式來擴大ResBlock模塊
