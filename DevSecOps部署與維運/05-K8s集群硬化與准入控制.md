# 05 · K8s 集群硬化與准入控制

> **學習目標**：掌握在 Kubernetes 集群中，透過 RBAC 最小化與 Kyverno 准入策略，阻斷不安全部署配置進入生產。

---

## 學習框架

### 一、為什麼需要集群硬化？

> 待補充
>
> 提示：CI 階段掃描鏡像 ≠ 就安全；惡意 YAML 配置（特權容器、不當 hostPath 掛載）仍可繞過前端防禦

---

### 二、K8s RBAC 最小特權

#### 2.1 RBAC 核心概念

| 資源 | 說明 |
|------|------|
| `Role` / `ClusterRole` | 待補充 |
| `RoleBinding` / `ClusterRoleBinding` | 待補充 |
| `ServiceAccount` | 待補充 |

#### 2.2 最小特權原則實踐

> 待補充

```yaml
# 待補充：範例 Role 配置
```

#### 2.3 常見錯誤配置

> 待補充
>
> 提示：wildcard `*` verb、cluster-admin 濫用

---

### 三、准入控制器（Admission Controller）

#### 3.1 什麼是准入控制？

> 待補充
>
> 提示：API 請求 → Authentication → Authorization → Admission Control → etcd

#### 3.2 Validating vs Mutating Webhook

| 類型 | 用途 | 執行時機 |
|------|------|---------|
| MutatingWebhook | 待補充 | 待補充 |
| ValidatingWebhook | 待補充 | 待補充 |

---

### 四、Kyverno 策略引擎

#### 4.1 為什麼選 Kyverno（而非 OPA/Rego）？

> 待補充
>
> 提示：YAML 語法 = K8s 原生，無需學習 Rego DSL

#### 4.2 核心資源類型

| 資源 | 作用範圍 |
|------|---------|
| `Policy` | 待補充（namespace 級別）|
| `ClusterPolicy` | 待補充（集群級別）|

#### 4.3 validationFailureAction 模式

| 模式 | 行為 |
|------|------|
| `Audit` | 待補充（觀察期，不阻斷）|
| `Enforce` | 待補充（直接攔截非合規部署）|

**建議實施流程**：先 Audit 觀察 1-2 週 → 修正違規 → 切換 Enforce

#### 4.4 常用策略範例

**禁止使用 `:latest` 標籤**

```yaml
# 待補充：check-image-tags 策略 YAML
```

**防禦目的**：待補充（版本追溯、避免引入未測試代碼）

**強制宣告資源限制**

```yaml
# 待補充：require-limits-configuration 策略 YAML
```

**防禦目的**：待補充（防止資源爭奪影響節點穩定性）

**其他常見策略**（待補充）

- [ ] 禁止特權容器（`privileged: true`）
- [ ] 禁止 `hostPath` 掛載
- [ ] 強制只允許特定 Registry 來源鏡像
- [ ] 禁止使用 `default` ServiceAccount

---

### 五、PSA / PSS（Pod Security Admission / Standards）

> 待補充
>
> 提示：K8s v1.25+ 內建的 Pod 安全標準，三個級別：Privileged / Baseline / Restricted

---

## 重點概念速查

- **准入控制器**：待補充
- **Kyverno ClusterPolicy**：待補充
- **最小特權原則**：待補充

---

## 延伸閱讀 / 參考資源

- [ ] Kyverno 官方文件：https://kyverno.io
- [ ] K8s RBAC 官方文件
- [ ] NSA/CISA Kubernetes Hardening Guide
- [ ] CKS 認證考綱
