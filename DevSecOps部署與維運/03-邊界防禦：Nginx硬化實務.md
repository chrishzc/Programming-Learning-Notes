# 03 · 邊界防禦：Nginx 硬化實務

> **學習目標**：掌握以 Nginx 作為安全反向代理的配置實踐，理解每個硬化指令背後的防禦邏輯。

---

## 學習框架

### 一、反向代理的安全角色

> 待補充
>
> 提示：不只是路由器，還是 TLS 終端、流量過濾、DoS 緩解、拓撲隱藏的防禦層

---

### 二、預設配置的問題

> 待補充
>
> 提示：Nginx 預設開放的資訊洩漏、過舊的 TLS 協定、無限制的請求緩衝區

---

### 三、核心硬化項目

#### 3.1 版本資訊隱藏

```nginx
# 待補充實際指令與說明
```

**防禦目的**：待補充

#### 3.2 緩衝區防禦（防 DoS / 緩衝區溢位）

```nginx
# 待補充實際指令與說明
```

**防禦目的**：待補充

#### 3.3 TLS 協定硬化

```nginx
# 待補充實際指令與說明
```

| 協定 | 狀態 | 原因 |
|------|------|------|
| TLS 1.0 | ❌ 禁用 | 待補充 |
| TLS 1.1 | ❌ 禁用 | 待補充 |
| TLS 1.2 | ✅ 允許 | 待補充 |
| TLS 1.3 | ✅ 允許 | 待補充 |

**常見協定漏洞**：BEAST、POODLE — 待補充說明

#### 3.4 加密套件配置

```nginx
# 待補充實際指令與說明
```

**防禦目的**：待補充

#### 3.5 安全回應標頭注入

| 標頭 | 防禦目標 | 值範例 |
|------|---------|-------|
| `X-Frame-Options` | 點擊劫持 | 待補充 |
| `X-Content-Type-Options` | MIME 探測 | 待補充 |
| `X-XSS-Protection` | XSS | 待補充 |
| `Content-Security-Policy` | 多種注入 | 待補充 |
| `Referrer-Policy` | 資訊洩露 | 待補充 |

#### 3.6 速率限制（防暴力破解 / DDoS）

```nginx
# 待補充實際指令與說明
```

**防禦目的**：待補充

---

### 四、完整硬化配置範例解析

> 待補充
>
> 提示：貼入完整 nginx.conf，逐段加上中文說明，對應到上方各硬化項目

---

### 五、SSL Session 優化

> 待補充（ssl_session_cache / ssl_session_timeout）

---

### 六、驗證方法

```bash
# 測試 TLS 協定是否拒絕舊版
# openssl s_client ...（待補充）

# 確認 Server 標頭是否隱藏
# curl -I ...（待補充）
```

---

## 重點概念速查

- **縱深防禦（Defense in Depth）**：待補充
- **TLS 終端（TLS Termination）**：待補充
- **upstream 健康檢查**：待補充

---

## 延伸閱讀 / 參考資源

- [ ] Mozilla SSL Configuration Generator
- [ ] Nginx 官方安全建議
- [ ] testssl.sh 工具
